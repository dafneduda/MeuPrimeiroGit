git init → criar o repositório local

git remote add origin 'link do github'

git add .

git commit -m "mensagem de commit" (toda vez que for fazer precisa "explicar" o motivo do commit)

git push origin main

criar branch nova: git checkout -b dev